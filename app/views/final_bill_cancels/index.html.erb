<div id="form_name_display">
	<table width="100%" border=0" cellspacing="0" cellpadding="0">
  	<tr>
    		<td width="50%">Payments>> IP >>&nbsp;&nbsp;&nbsp;<a HREF='final_bill_cancels/new' class="nav" style="text-decoration:none;">Final Bill Cancel</a></td>
    		<td align="right" width="50%"><%= format_date(:short, Date.today ) %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
  	</tr>
	</table>
</div>
<br/>
<br/>

<center>
<%= link_to 'New', new_final_bill_cancel_path, :class=>"new1"  %>
</center>
<script type="application/javascript">
var type
	function clear_file()
	{
		document.getElementById("admn_no").value=""
		document.getElementById("mr_no").value=""
		document.getElementById("final_bill_amount").value=""
		document.getElementById("final_bill_cancel_amount").value=""
	}
 </script>
<div align="center" width="100%">
<table class="tablesorter" border="0" cellpadding="0" cellspacing="1" >
<tbody>
	<tr>
	<td width="12%">&nbsp;</td>

	
	<td width="15%"><input type="text" style="width:100%" id="admn_no" onKeyUp = "search_function(id,event)" placeholder="Search Admn.No"></td>
					<%= javascript_include_tag:defaults %>
					<%= observe_field 'admn_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&final_bill_amount=' + escape($('final_bill_amount').value)+ '&final_bill_cancel_amount=' + escape($('final_bill_cancel_amount').value) " %>
					  
	<td width="15%"><input type="text" style="width:100%" id="mr_no" onKeyUp = "search_function(id,event)" placeholder="Search MR No"></td>
					<%= observe_field 'mr_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&final_bill_amount=' + escape($('final_bill_amount').value)+ '&final_bill_cancel_amount=' + escape($('final_bill_cancel_amount').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="final_bill_amount" onKeyUp = "search_function(id,event)" placeholder="Search Final Bill Amount"></td>
					<%= observe_field 'final_bill_amount', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&final_bill_amount=' + escape($('final_bill_amount').value)+ '&final_bill_cancel_amount=' + escape($('final_bill_cancel_amount').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="final_bill_cancel_amount" onKeyUp = "search_function(id,event)" placeholder="Search Cancel Amount"></td>
					<%= observe_field 'final_bill_cancel_amount', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&final_bill_amount=' + escape($('final_bill_amount').value)+ '&final_bill_cancel_amount=' + escape($('final_bill_cancel_amount').value) " %>
	<td colspan="3" align="center" valign="top">
		<%= link_to 'Clear','', :class=>"new1",:onClick => "clear_file()"  %>
	</td>
  </tr>
 </tbody>
</table>
</div>

 
 <!-- End of the Code for field search	 -->
 
<div id="name" align="center" width="100%">
<table id="tablesorter-demo" class="tablesorter" border="0" cellpadding="0" cellspacing="1">
<thead>
<tr>
    <th>S.No</th>
    <th>Admn.No</th>
    <th>MR No</th>
    <th>Final bill amount</th>
    <th>Final bill cancel amount</th>
	<th colspan="2" align="center">Permissions</th>
</tr>
 </thead>
<tbody>
<% @i=0 %>
<% @final_bill_cancel.each do |final_bill_cancel| %>
  <% if((@i%2)==0) 
		id="datacelltwo"
	else
		id="datacellone"
  end %>
  <tr class='<%= id %>' style="height:30px">
    <td><%= @i=@i+1 %></td>
    <td><%=h final_bill_cancel.admn_no %></td>
    <td><%=h final_bill_cancel.mr_no %></td>
    <td><%=h final_bill_cancel.final_bill_amount %></td>
    <td><%=h final_bill_cancel.final_bill_cancel_amount %></td>

	<td align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Edit&nbsp;&nbsp;&nbsp;', edit_final_bill_cancel_path(final_bill_cancel), :class=>"submit_button" %></td>   
	<td  align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Print&nbsp;&nbsp;&nbsp;', {:action => 'report', :id => final_bill_cancel.id, :format =>"pdf" , :print_type=>"duplicate"}, :class=>"submit_button" %></td>
	
  </tr>
<% end %>
</tbody>
</table>
<br />
</div>


<p align="right"><div align="right" class="pagination"><%= will_paginate @final_bill_cancel,  :style => "font: bold 15px Arial red;" %></p>
