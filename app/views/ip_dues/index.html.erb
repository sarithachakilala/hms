<div id="form_name_display">
	<table width="100%" border=0" cellspacing="0" cellpadding="0">
  	<tr>
    		<td width="50%">Payments>> IP >>&nbsp;&nbsp;&nbsp;<a HREF='ip_dues/new' class="nav" style="text-decoration:none;">Ip Dues</a></td>
    		<td align="right" width="50%"><%= format_date(:short, Date.today ) %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
  	</tr>
	</table>
</div>
<br/>
<br/>

<center>
<%= link_to 'New', new_ip_due_path, :class=>"new1" %>
</center>
<script type="application/javascript">
var type
	function clear_file()
	{
		document.getElementById("admn_no").value=""
		document.getElementById("mr_no").value=""
		document.getElementById("due_date").value=""
		document.getElementById("receipt_no").value=""
		document.getElementById("gross_amount").value=""
		document.getElementById("due").value=""
	}
 </script>
<div align="center" width="100%">
<table class="tablesorter" border="0" cellpadding="0" cellspacing="1" >
<tbody>
	<tr>
	<td width="12%">&nbsp;</td>

	
	<td width="15%"><input type="text" style="width:100%" id="admn_no" onKeyUp = "search_function(id,event)" placeholder="Search Admn.No"></td>
					<%= javascript_include_tag:defaults %>
					<%= observe_field 'admn_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
					  
	<td width="15%"><input type="text" style="width:100%" id="mr_no" onKeyUp = "search_function(id,event)" placeholder="Search MR No"></td>
					<%= observe_field 'mr_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="due_date" onKeyUp = "search_function(id,event)" placeholder="Search Due Date"></td>
					<%= observe_field 'due_date', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="receipt_no" onKeyUp = "search_function(id,event)" placeholder="Search Receipt No"></td>
					<%= observe_field 'receipt_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="gross_amount" onKeyUp = "search_function(id,event)" placeholder="Search Gross Amount"></td>
					<%= observe_field 'gross_amount', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="due" onKeyUp = "search_function(id,event)" placeholder="Search Due"></td>
					<%= observe_field 'due', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&due_date=' + escape($('due_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&gross_amount=' + escape($('gross_amount').value)+ '&due=' + escape($('due').value) " %>
	
	<td colspan="3" align="center" valign="top">
		<%= link_to 'Clear','', :class=>"new1",:onClick => "clear_file()"  %>
	</td>
  </tr>
 </tbody>
</table>
</div>

 
 <!-- End of the Code for field search	 -->
 
<div id="name" align="center" width="100%">
<table id="tablesorter-demo" class="tablesorter" border="0" cellpadding="0" cellspacing="1">
<thead>
  <tr>
    <th>S.No</th>
    <th>Admn.No</th>
    <th>MR No</th>
    <th>Date</th>
    <th>Bill No</th>
    <th>Gross Amount</th>
    <th>Due</th>
	<th align="center" colspan="2">Permissions</th>
  </tr>
  </thead>
<tbody>
<% @i=0 %>
<% @ip_due.each do |ip_due| %>
  <% if((@i%2)==0) 
		id="datacelltwo"
	else
		id="datacellone"
  end %>
  <tr class='<%= id %>' style="height:30px">
    <td><%= @i=@i+1 %></td>
    <td><%=h ip_due.admn_no %></td>
    <td><%=h ip_due.mr_no %></td>
    <td><%=h ip_due.due_date %></td>
    <td><%=h ip_due.receipt_no %></td>
    <td><%=h ip_due.gross_amount %></td>
    <td><%=h ip_due.due %></td>

	<td align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Edit&nbsp;&nbsp;&nbsp;', edit_ip_due_path(ip_due), :class=>"submit_button" %></td>   
	<td  align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Print&nbsp;&nbsp;&nbsp;', {:action => 'report', :id => ip_due.id, :format =>"pdf" , :print_type=>"duplicate"}, :class=>"submit_button" %></td>
	
 </tr>
<% end %>
</table>

<br />

<p align="right"><div align="right" class="pagination"><%= will_paginate @ip_due,  :style => "font: bold 15px Arial red;" %></p>
