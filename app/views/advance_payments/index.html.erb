<div id="form_name_display">
	<table width="100%" border=0" cellspacing="0" cellpadding="0">
  	<tr>
    		<td width="50%">Payments>> IP >>&nbsp;&nbsp;&nbsp;<a HREF='/advance_payments' class="nav" style="text-decoration:none;">Advance Payment</a></td>
    		<td align="right" width="50%"><%= format_date(:short, Date.today ) %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
  	</tr>
	</table>
</div>
<div id="form_data">
<br/>
<center>
<%= link_to 'New', new_advance_payment_path, :class=>"new1" %>
</center>
<script type="application/javascript">
var type
	function clear_file()
	{
		document.getElementById("admn_no").value=""
		document.getElementById("mr_no").value=""
		document.getElementById("advance_date").value=""
		document.getElementById("receipt_no").value=""
		document.getElementById("gross_amount").value=""
		document.getElementById("already_paid_amount").value=""
	}
 </script>
<div align="center" width="100%">
<table class="tablesorter" border="0" cellpadding="0" cellspacing="1" >
<tbody>
	<tr>
	<td width="5%">&nbsp;</td>

	
	<td width="15%"><input type="text" style="width:100%" id="admn_no" onKeyUp = "search_function(id,event)" placeholder="Search Admn.No"></td>
					<%= javascript_include_tag:defaults %>
					<%= observe_field 'admn_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
					  
	<td width="15%"><input type="text" style="width:100%" id="mr_no" onKeyUp = "search_function(id,event)" placeholder="Search MR No"></td>
					<%= observe_field 'mr_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="advance_date" onKeyUp = "search_function(id,event)" placeholder="Search Date"></td>
					<%= observe_field 'advance_date', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="receipt_no" onKeyUp = "search_function(id,event)" placeholder="Search Receipt No"></td>
					<%= observe_field 'receipt_no', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
	<td width="15%"><input type="text" style="width:100%" id="gross_amount" onKeyUp = "search_function(id,event)" placeholder="Search Gross Amount"></td>
					<%= observe_field 'gross_amount', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
		<td width="15%"><input type="text" style="width:100%" id="already_paid_amount" onKeyUp = "search_function(id,event)" placeholder="Search Already Paid Amount"></td>
					<%= observe_field 'already_paid_amount', :frequency => 0.01,
					:update => :name,
					:url => {:action => :observe_field_ex },
					:with => "'admn_no='+escape($('admn_no').value)+ '&mr_no=' + escape($('mr_no').value)+ '&advance_date=' + escape($('advance_date').value)+ '&receipt_no=' + escape($('receipt_no').value)+ '&already_paid_amount=' + escape($('already_paid_amount').value)+ '&gross_amount=' + escape($('gross_amount').value) " %>
	
	<td colspan="3" align="center" valign="top">
		<%= link_to 'Clear','', :class=>"new1",:onClick => "clear_file()"  %>
	</td>
  </tr>
 </tbody>
</table>
</div>

 
 <!-- End of the Code for field search	 -->
 
<div id="name" align="center" width="100%">
<table id="tablesorter-demo" class="tablesorter" border="0" cellpadding="0" cellspacing="1">
<thead>
  <tr class="info">
    <th>S.No</th>
	<th>Admn.No</th>
    <th>MR No</th>
    <th>Date</th>
	<th>Receipt No</th>
    <th>Gross Amount</th>
    <th>Already paid amount</th>
    <th colspan="2" align="center">Permissions</th>
  </tr>
</thead>
<tbody>
<% @i=0 %>
<% @advance_payment.each do |advance_payment| %>
  <% if((@i%2)==0) 
		id="datacelltwo"
	else
		id="datacellone"
  end %>
  <tr class='<%= id %>' style="height:30px">
    <td><%= @i=@i+1 %></td>
	<td><%=h advance_payment.admn_no %></td>
    <td><%=h advance_payment.mr_no %></td>
    <td><%=h advance_payment.advance_date %></td>
	<td><%=h advance_payment.receipt_no %></td>
    <td><%=h advance_payment.gross_amount %></td>
    <td><%=h advance_payment.already_paid_amount %></td>
 
	<td align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Edit&nbsp;&nbsp;&nbsp;', edit_advance_payment_path(advance_payment), :class=>"submit_button" %></td>   
	<td  align="center"><%= link_to '&nbsp;&nbsp;&nbsp;Print&nbsp;&nbsp;&nbsp;', {:action => 'report_bill', :id => advance_payment.id, :format =>"pdf" , :print_type=>"duplicate"}, :class=>"submit_button" %></td>

	
<% end %>
</tbody>
</table>
<br />
</div>

<p align="right"><div align="right" class="pagination"><%= will_paginate @advance_payment,  :style => "font: bold 15px Arial red;" %></p>
